<?xml version="1.0" encoding="utf-8"?>
<shape
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners
        android:radius="10dp"/>

    <!--solid
        android:color="@color/border_color" /-->

    <gradient
        android:angle="-90"
        android:centerX="50%"
        android:centerY="50%"
        android:centerColor="@color/button_start_color"
        android:gradientRadius="200%"
        android:startColor="@color/button_centre_color"
        android:type="linear"
        android:endColor="@color/button_end_color"/>

        <!--android:useLevel=["true" | "false"]-->

    <!--padding
        android:left="integer"
        android:top="integer"
        android:right="integer"
        android:bottom="integer" /-->
    <size
        android:width="200dp"
        android:height="50dp" />

    <stroke
        android:width="2dp"
        android:color="@color/buton_border_color"/>
        <!--android:dashWidth="25dp"
        android:dashGap="5dp" /-->
</shape>
